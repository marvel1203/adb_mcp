{
  "name": "adb-mcp",
  "version": "0.1.0",
  "description": "A Model Context Protocol (MCP) server for Android Debug Bridge (ADB)",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "adb-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "keywords": [
    "mcp",
    "adb",
    "android",
    "debug",
    "bridge",
    "model-context-protocol"
  ],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
